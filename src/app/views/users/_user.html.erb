<tr>
  <td><%= link_to user.name, user %></td>
  <td><%= user.username %></td>
  <% if current_user.admin? && (not ( current_user == user))  %>
      <td><%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %></td>
  <% end %>
</tr>
